// Phase 2 — Prompt Loader
// Loads agent prompt markdown from /agents/*.md at runtime.
// Prompts are files, not compiled code — update prompts without rebuilding.

export function loadPrompt(promptFile: string, runtimeRoot: string): string {
  throw new Error('Not implemented')
}
